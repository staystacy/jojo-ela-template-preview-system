{
  "name": "jojo-ela-template-preview-system",
  "version": "0.2.0",
  "private": true,
  "description": "JOJO ELA Worksheet Template Preview Tool — internal review system for 16 worksheet templates + Bitable rendering mode",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.0"
  }
}
